export interface Product {
    id: number,
    title: string,
    price: number,
    quantity: number,
    category: string,
    sub_total: number,
    image: string,
    sku: string,
    stock: number
}